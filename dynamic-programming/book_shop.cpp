#include <iostream>
#include <vector>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n, x;
    cin >> n >> x;

    vector<int> books, pages;

    for (int &b : books) {
        cin >> b;
    }

    for (int &p : pages) {
        cin >> p;
    }

    
}